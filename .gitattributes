# 换行符策略
#
# 这一条放在最前，作为默认：源码与数据统一 LF。
* text=auto eol=lf

# Windows 批处理必须是 CRLF，且内容必须纯 ASCII、无 BOM。
# 纯 LF 会让 goto :LABEL 跳转失效，BOM 会让首行变成乱码命令。
# 这里强制覆盖上面的默认规则，避免 git 在检出时改动它。
*.bat text eol=crlf

# 二进制资源不做任何转换
*.woff2 binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary

# 字体/图片之外的资源保持文本
*.svg text
